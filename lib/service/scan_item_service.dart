import '../provider/scan_item_provider.dart';

class ScanItemService {
  /*
  final ScanItemProvider provider;

  ScanItemService(this.provider);

  Future<void> addScanData(String data) async {
    await provider.addData(data);
  }

  Future<void> addCodeDetail(String key, Map<String, dynamic> details) async {
    await provider.addCodeDetails(key, details);
  }

  Future<void> removeScanData(String data) async {
    await provider.removeData(data);
  }

  Future<void> removeCodeDetail(String key) async {
    await provider.removeCodeDetails(key);
  }

  */
}
